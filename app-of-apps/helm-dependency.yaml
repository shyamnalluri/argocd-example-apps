apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps-helm-dependency
  namespace: guestbook
spec:
  project: default
  source:
    repoURL: https://github.com/shyamnalluri/argocd-example-apps
    targetRevision: master
    path: ./helm-dependency
  destination:
    server: https://kubernetes.default.svc
    namespace: guestbook
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
